# Configurations

Pinned kernel, BusyBox and libc configuration fragments belong here.

M0 will add target-specific configurations only after the corresponding
toolchain/kernel combination has been qualified. Do not copy generic distro
configs into MikrOS merely for convenience.
